<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s_ahzzng.youandme">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.DIAL_PHONE" />


    출처: http://mainia.tistory.com/4884 [녹두장군 - 상상을 현실로]
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Choice"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Senior.Senior_menu" />
        <activity android:name=".Student.Student_login" />
        <activity
            android:name=".Student.Student_main"
            android:label="Sharing House"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Student.Student_homedetail" />
        <activity android:name=".Title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Test.Test_title" />
        <activity android:name=".Test.Test" />
        <activity android:name=".Senior.Senior_emergency"></activity>
    </application>

</manifest>